import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  normalText: {
    fontSize: 16
  },
  title: {
    fontSize: 20,
    fontWeight: 'bold'
  },
  textWhite: {
    color: '#fff'
  }
})
